from civic_sandbox.map_layer_maker import SandboxMaker 


poi_layer = SandboxMaker(
    layer_key = 'disaster_resilience_18_001',
    )

shaking_layer = SandboxMaker(
    layer_key = 'disaster_resilience_18_002',
    )

liquefaction_layer = SandboxMaker(
    layer_key = 'disaster_resilience_18_003',
    )

landslide_layer = SandboxMaker(
    layer_key = 'disaster_resilience_18_004',
    )

census_response_layer = SandboxMaker(
    layer_key = 'disaster_resilience_18_005',
    )